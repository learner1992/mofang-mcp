[project]
name = "mofang-skill-v2"
version = "0.1.0"
description = "Mofang enterprise query MCP server"
requires-python = ">=3.11"
dependencies = [
  "PyYAML>=6.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]

